from statistics import mean

def staff(color):
    var ={}

    for color in color:
        if not var == color:

            var['1'] == color
color = ['black', 'blue', 'white', 'orange','gray']
ans = staff(color)
print(ans)

